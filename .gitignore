*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.kotlin/
.DS_Store
/build
# Covers every module's build/ output (app/ also has its own nested .gitignore for this).
**/build/

/captures
.externalNativeBuild
.cxx
local.properties

# Repo-local scratch/work dir for build logs etc. (survives WSL crashes that wipe /tmp).
# Wipe with ./clean_tmp.sh
/tmp/

# 生成物なのでテンプレートapkも追加
app/src/main/assets/keyboard_template.apk