<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".KeyboardFactoryApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidKeyboardFactory">
        <activity
            android:name=".ui.ProjectListActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AndroidKeyboardFactory">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Best-effort: lets a file manager's "open with" launch import for a tapped
                 .keyboard file. .keyboard has no MIME type registered with Android, so which of
                 these a given file manager actually matches (mimeType vs. pathPattern, and what
                 URI scheme/shape it hands off) varies; this is not guaranteed on every device. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.keyboard" />
                <data android:host="*" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.EditorActivity"
            android:exported="false"
            android:theme="@style/Theme.AndroidKeyboardFactory" />
    </application>

</manifest>